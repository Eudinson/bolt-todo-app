{
  "name": "bolt-todo-app",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix front\" \"npm run dev --prefix back\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
